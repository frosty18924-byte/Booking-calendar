export const metadata = {
  title: 'IT Support Referral',
  description: 'Submit IT support requests with troubleshooting guides',
};

export default function ITReferralLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
