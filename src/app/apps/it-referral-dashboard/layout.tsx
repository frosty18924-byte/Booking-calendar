export const metadata = {
  title: 'IT Referrals Dashboard',
  description: 'View and manage IT support referrals',
};

export default function ITReferralDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
